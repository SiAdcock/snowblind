'use strict';

import React from 'react';
import Viewport from '../components/viewport';

class GameContainer {
  render() {

    return (
      <div>
        <h1>Snowblind</h1>
        <Viewport/>
      </div>
    );
  }
}

export default GameContainer;
