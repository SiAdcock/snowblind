'use strict';

const expect = require('chai').expect;
const HTMLComponent = require('../../../app/modules/index');

describe('Index HTML Component', () => {
  beforeEach(() => {

  });
  it('renders its markup props', () => {
    expect(1).to.equal(1);
  });
});
